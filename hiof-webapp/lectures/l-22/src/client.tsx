import { initClient, initClientNavigation } from "rwsdk/client";

initClient();
//initClientNavigation({ scrollBehavior: "smooth" }); // Bug: with this on, the Location header is not resolved
//initClientNavigation({ scrollBehavior: "smooth" });
