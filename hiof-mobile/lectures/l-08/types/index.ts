export type Student = {
	id: string;
	name: string;
	program: string;
	expireAt: string;
	role: string;
	isActive: boolean;
};

export type Theme = {
	primary: string;
	secondary: string;
	text: string;
	background: string;
};
